analyticssingle:
  build: docker/analytics/single/
  ports:
    - "127.0.0.1:49000:22"
  environment:
    - GITHUB_USERNAME
    - "MR_CLUSTER_ROLES=master,core"
  volumes:
    - /mnt/analytics-docker:/var/tmp/analytics:ro

