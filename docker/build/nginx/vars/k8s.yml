nginx_lms_gunicorn_hosts:
  - "{{ release_name | default('') }}-lms"
nginx_lms_preview_gunicorn_hosts:
  - "{{ release_name | default('') }}-lms"
nginx_cms_gunicorn_hosts:
  - "{{ release_name | default('') }}-cms"
