- name: Configure instance(s)
  hosts: all
  sudo: True
  gather_facts: True
  vars_files:
    - "group_vars/all"
  roles:
    - edx_ansible
    - edxlocal
    - mongo
    - edxapp
    - oraclejdk
    - elasticsearch
    - forum
    - ora
    - browsers
    - local_dev
    - demo
