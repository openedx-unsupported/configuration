---
- hosts: localhost
  gather_facts: False
  vars:
    EDXAPP_LMS_NGINX_PORT: '80'
    EDXAPP_LMS_NGINX_PORT: '80'
  roles: 
    - role: nginx
      nginx_sites:
      - lms
      - cms
      nginx_default_site:
      - lms
    - lets_encrypt

