- name: Deploy dbt-docs server
  hosts: all
  become: True
  gather_facts: True
  roles:
    - aws
    - nginx
    - dbt_docs_nginx
    - role: aws_cloudwatch_agent
      vars:
          template_src: cloudwatch-agent-metrics-config.json