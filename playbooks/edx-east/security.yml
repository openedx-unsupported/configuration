# To Run just the check for known vulnerabilities
# ansible-playbook -c ssh -i inventory.ini security.yml --tags check -e "SECURITY_CHECK_ONLY=true"

# To Run and make updates if issues found
# ansible-playbook -c ssh -i inventory.ini security.yml
- name: Security
  hosts: all
  sudo: True
  gather_facts: True
  roles:
    - security
