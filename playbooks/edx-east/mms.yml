---

- name: Test MMS role
  hosts: all
  connection: local
  gather_facts: False
  vars:
    state: "present"
  tasks:
    - name: Test MMS
      mms_alert:
        base_url: 'https://cloud.mongodb.com'
        group_id: 'foo'
        api_user: 'ed@example.com'
        api_key: 'xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx'
      register: api_output

    - debug: var=api_output