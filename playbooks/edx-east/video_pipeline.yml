- name: Deploy edX Video Pipeline Service
  hosts: all
  become: True
  gather_facts: True
  vars:
    ENABLE_NEWRELIC: False
    migrate_db: 'yes'
  roles:
    - role: nginx
      nginx_default_sites:
        - video_pipeline
    - aws
    - role: video_pipeline
