---
#
# edX Configuration
#
# github:     https://github.com/edx/configuration
# wiki:       https://openedx.atlassian.net/wiki/display/OpenOPS
# code style: https://openedx.atlassian.net/wiki/display/OpenOPS/Ansible+Code+Conventions
# license:    https://github.com/edx/configuration/blob/master/LICENSE.TXT
#
##
# Defaults for role themes
##  This role is not meant to be included in a play.  It should be included in the meta vars of 
#another role.  When included, it should contain a dict of repos:
#   - role: git_checkout
#     GIT_CHECKOUT_REPOS:
#     	- PROTOCOL: "{{ COMMON_GIT_PROTOCOL }}"
#        	DOMAIN: "{{ COMMON_GIT_MIRROR }}"
#        	PATH: "{{ COMMON_GIT_PATH }}"
#       	REPO: "{{ SERVICE_REPO }}"
#        	VERSION: "{{ SERVICE_VERSION }}"
#        	DESTINATION: "{{ SERVICE_CODE_DIR }}"
#        	SSH_KEY: "{{ SERVICE_GIT_IDENTITY }}"
#             OWNER: "{{ service_user }}"
