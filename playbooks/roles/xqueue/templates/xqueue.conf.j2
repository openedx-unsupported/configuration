[program:xqueue]

command={{ xqueue_supervisor_wrapper }}

user={{ common_web_user }}
directory={{ xqueue_code_dir }}
stdout_logfile={{ supervisor_log_dir }}/%(program_name)-stdout.log
stderr_logfile={{ supervisor_log_dir }}/%(program_name)-stderr.log
killasgroup=true
stopasgroup=true
