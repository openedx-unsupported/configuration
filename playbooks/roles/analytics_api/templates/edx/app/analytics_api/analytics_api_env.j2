# {{ ansible_managed }}

source {{ analytics_api_venv_dir }}/bin/activate
{% for name,value in analytics_api_environment.items() -%}
{%- if value -%}
export {{ name }}="{{ value }}"
{% endif %}
{%- endfor %}
