- name: Deploy {{ my_role_name|replace('_', ' ')|title }}
  hosts: all
  sudo: True
  gather_facts: True
  vars:
    serial_count: 1
  serial: "{{ '{{' }} serial_count }}"
  roles:
    - nginx
    - role: {{ my_role_name }}
      nginx_default_sites:
        - {{ my_role_name }}
