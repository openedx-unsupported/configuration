- name: Deploy {{ role_name|replace('_', ' ')|title }}
  hosts: all
  sudo: True
  gather_facts: True
  vars:
    serial_count: 1
  serial: "{{ '{{' }} serial_count }}"
  roles:
    - nginx
    - role: {{ role_name }}
      nginx_default_sites:
        - {{ role_name }}