---

- name: grab the war file from s3
  s3: >
    bucket=edx-static
    object=/devops/jenkins_assets/asgard.war.{{ HOTG_GIT_REVISION }}/asgard.war
    dest=/var/lib/tomcat7/webapps/ROOT.war mode=get overwrite=true
  notifiy:
    - restart hotg