# {{ ansible_managed }}

{% for name,value in forum_environment.items() -%}
{%- if value|default(None) != None and value != "" -%}
export {{ name }}="{{ value }}"
{% endif %}
{%- endfor %}

eval "$(rbenv init -)"
