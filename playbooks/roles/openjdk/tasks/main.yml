---
# oraclejdk
#
# Example play:
#
#   roles:
#   - oraclejdk

- name: OpenJDK has been installed with its dependencies
  apt:
    name: "openjdk-{{OPENJDK_MAJOR_VERSION}}-jdk"
    update_cache: true
