- name: Configure instance(s)
  hosts: edx_gce
  sudo: True
  gather_facts: True
  vars:
    migrate_db: True
    mysql5_workaround: True
  roles:
    - common
    - nginx
    - edxlocal
    - edxapp
    # drops sysvinit scripts for compute engine
    - gce
